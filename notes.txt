During querying the database the query operator can only return on 
    value if the product for similar names in the ciolumn labelled primary key

query with the method with entities is usedf to get a specific column

use the command to exit safe mode to delete tables in mysql
    SET SQL_SAFE_UPDATES = 0;

enable auto increment in index columns and uniqueness

In js used checked prop on checkbox to get if checkbox is checked